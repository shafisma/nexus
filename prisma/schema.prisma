generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Message {
  id        String   @id @default(cuid())
  content   String
  userId    String
  userName  String
  createdAt DateTime @default(now())
}
